syntax = "proto3";

package transaction;

option go_package = "./transaction";

message SortColumn {
    string column = 1;
    string order = 2;
}

message SortRequest {
    repeated SortColumn columns = 1;
}